name: Cleanup Duplicate Files

on:
  # Run manually only — never automatically
  workflow_dispatch:
    inputs:
      dry_run:
        description: >
          DRY RUN mode?
          true  = just show what would be moved (safe to test)
          false = actually move duplicate files to duplicates/ folder
        required: true
        default: 'true'
        type: choice
        options:
          - 'true'
          - 'false'

permissions:
  contents: write

jobs:
  cleanup:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Setup Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install openpyxl google-auth google-auth-oauthlib google-api-python-client

      - name: Pull latest state
        run: git pull origin main || true

      - name: Run cleanup
        env:
          GOOGLE_CLIENT_ID:      ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET:  ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_REFRESH_TOKEN:  ${{ secrets.GOOGLE_REFRESH_TOKEN }}
          GDRIVE_PSD_FOLDER:     ${{ secrets.GDRIVE_PSD_FOLDER }}
        run: |
          if [ "${{ github.event.inputs.dry_run }}" = "true" ]; then
            echo "Running in DRY RUN mode — showing what would be moved"
            python scripts/cleanup_duplicates.py --dry-run --drive --preview
          else
            echo "Running in LIVE mode — moving duplicates to duplicates/ folder"
            python scripts/cleanup_duplicates.py --no-dry-run --drive --preview
          fi

      - name: Commit cleaned preview_image folder
        if: ${{ github.event.inputs.dry_run == 'false' }}
        run: |
          git config user.name  "PSD-Bot"
          git config user.email "psdbot@automation.local"
          git add preview_image/ 2>/dev/null || true
          git diff --cached --quiet || \
            git commit -m "🧹 Cleanup: move duplicate preview files to duplicates/ $(date '+%Y-%m-%d %H:%M UTC')"
          git push origin main || true

      - name: Summary
        if: always()
        run: |
          echo "## Cleanup Summary" >> $GITHUB_STEP_SUMMARY
          echo "- Mode: ${{ github.event.inputs.dry_run == 'true' && 'DRY RUN (preview only)' || 'LIVE (moved to duplicates/)' }}" >> $GITHUB_STEP_SUMMARY
          echo "- Completed at: $(date -u)" >> $GITHUB_STEP_SUMMARY
          WEBP_COUNT=$(find preview_image/ -maxdepth 1 -name "*.webp" 2>/dev/null | wc -l)
          DUP_COUNT=$(find preview_image/duplicates/ -name "*.webp" 2>/dev/null | wc -l)
          echo "- Preview files remaining: $WEBP_COUNT" >> $GITHUB_STEP_SUMMARY
          echo "- Files in duplicates/: $DUP_COUNT" >> $GITHUB_STEP_SUMMARY
